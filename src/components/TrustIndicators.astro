---
import { STATS } from '../data/content';

const delays = ['delay-100', 'delay-200', 'delay-300', 'delay-400'];
---

<section class="relative z-20 -mt-20 md:-mt-24 pb-16 md:pb-24 px-6">
  <div class="container mx-auto max-w-7xl">
    <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 md:gap-8">
      {
        STATS.map((stat, i) => (
          <div
            class:list={[
              'bg-white/90 backdrop-blur-xl p-6 lg:p-8 rounded-3xl shadow-premium border border-white/40 flex flex-col items-center text-center group hover:-translate-y-2 transition-transform duration-500 reveal',
              delays[i],
            ]}
          >
            <span class="text-3xl md:text-4xl lg:text-5xl font-serif text-primary mb-2 group-hover:text-secondary transition-colors">
              {stat.value}
            </span>
            <span class="text-xs md:text-sm font-medium text-accent uppercase tracking-wider">
              {stat.label}
            </span>
          </div>
        ))
      }
    </div>
  </div>
</section>
